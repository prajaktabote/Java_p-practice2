//method:-public string replace(char oldChar,char new char);
//replace every instance of character in string with new character.)
//parameter:-character(oldchar).character(newChar);
//return Type:= String
class StringReplaceDemo
{
    public static void main(String args[])
    {
        String str1 = "Shashi";

        System.out.println(str1.toReplace('h','s'));
    }
}